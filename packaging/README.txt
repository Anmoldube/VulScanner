Packaging notes:
- On Linux use CPack (cmake -DCPACK_GENERATOR=DEB ..) or create DEB/RPM with fpm.
- On Windows use WiX to create MSI installers.
- On macOS use pkgbuild/productbuild to create .pkg and create-dmg for .dmg.
